# Copy to .env and adjust. Never commit your real .env.

# Logging verbosity: debug | info | warn | error
LOG_LEVEL=info

# Where the note_* tools persist data (relative or absolute path).
STATE_DIR=state
