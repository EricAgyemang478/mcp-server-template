{
  "//": "Example MCP client config (e.g. Claude Desktop). Point command/args at this server's built entrypoint.",
  "mcpServers": {
    "template": {
      "command": "node",
      "args": ["/absolute/path/to/mcp-server-template/dist/index.js"],
      "env": {
        "LOG_LEVEL": "info"
      }
    }
  }
}
